#include <stdio.h>

int main(){

	int tamanho = 0;

	printf("Informe o tamanho de um lado do quadrado \n");

	scanf("%d", &tamanho);

	printf("A area do quadrado e: %d", (tamanho * tamanho));

	return 0;
}
