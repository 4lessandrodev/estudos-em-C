#include <stdio.h>

int main(){

	int a, b, c;

	printf("Informe 3 numeros \n");

	scanf("%d", &a);
	scanf("%d", &b);
	scanf("%d", &c);

	printf("O resultado da soma dos valores e: %d", (a+b+c));

	return 0;
}
